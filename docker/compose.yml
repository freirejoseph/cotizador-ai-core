services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: cotizador-ai-core-app
    volumes:
      - /srv/projects/cotizador-ai-core:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true
    restart: unless-stopped
